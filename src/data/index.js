const { nouns } = require("./nouns-list");
const { adjectives } = require("./adjectives-list");
const { marvel } = require("./marvel");
const { dc } = require("./dc");

module.exports = {
  1: nouns,
  2: adjectives,
  3: marvel,
  4: dc,
};
